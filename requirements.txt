huggingface_hub>=0.23.0
requests>=2.31.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
numpy>=1.26.0
pgmpy>=0.1.25

# ── Vector store ──────────────────────────────────────────────────────────────
qdrant-client>=1.9.0          # Qdrant (primary vector DB)
chromadb>=0.5.0               # ChromaDB (fallback / in-process dev mode)
sentence-transformers>=3.0.0  # Local embeddings (all-MiniLM-L6-v2, ~80 MB)
